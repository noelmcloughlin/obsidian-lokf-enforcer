# vscode
.vscode

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# It is uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# local sample/test vault (never published)
sample-vault/

# OS/editor noise
*.swp

# Local tooling, if a contributor runs npm/npx here directly
node_modules/

# local sample/test vault (never published)
-sample-vault/

# .agents/skills/ + skills-lock.json are committed (installed LOKF skills the
# repo depends on, pinned by hash - see MYPLAN.md Phase 3); .claude/ is local
# session state only.
.claude/
